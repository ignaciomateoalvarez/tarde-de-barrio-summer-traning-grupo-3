<%# publications.each do |key,publication_decorator| %>
  <%# publication_decorator.each do |publication| %>
    <%#= render partial: "home_partials/publication", locals: { publication: publication, featured: featured } %>
    <%= render partial: "home_partials/publication", locals: { publication: 1 } %>
    <%= render partial: "home_partials/publication", locals: { publication: 2 } %>
  <%# end %>
<%# end %>