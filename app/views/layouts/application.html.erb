<!DOCTYPE html>
<html>
  <head>
    <title>StGrupo3</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <% flash.each do |type, msg| %>
      <%= render '/partials/flash_message' , message: msg, warning_type: type %>
    <% end %>
    <%= turbo_frame_tag "modal", user: @user %>
    <%= render partial: '/partials/navbar' if (logged_in? && not(current_page?('/login') || current_page?('/register'))) %>
    <%= yield %>
  </body>
</html>
